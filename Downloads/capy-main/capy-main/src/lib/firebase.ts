// Mock stub — Firebase is temporarily inaccessible.
// All auth/db/storage functionality is handled by local mock layers.
// To restore: git checkout src/lib/firebase.ts

export const auth = null;
export const db = null;
export const storage = null;
export default null;
